# F3 Parlay AI - Environment Variables
# Copy this file to .env in the root directory if you need root-level config

# Backend Configuration
BACKEND_URL=http://localhost:8000

# Frontend Configuration
FRONTEND_URL=http://localhost:3000

# Development Mode
NODE_ENV=development

# -----------------------------------------------------------------------------
# Custom Builder: Counter Ticket & Coverage Pack (backend/app/core/config.py)
# Flip one at a time: Counter first, then Coverage. Rollback = set to false.
# -----------------------------------------------------------------------------
# FEATURE_COUNTER_TICKET=true
# FEATURE_COVERAGE_PACK=true
