# F3 Parlay AI - Environment Variables
# Copy this file to .env in the root directory if you need root-level config
#
# IMPORTANT - Oracle / VM deploy (docker-compose.prod.yml):
# The ROOT .env is loaded into the api and verifier containers. It must contain
# ALL backend variables. Copy or merge from backend/.env (or backend/.env.example).
# Minimum required for api/verifier to start: DATABASE_URL, THE_ODDS_API_KEY,
# plus REDIS_URL in production, and any other vars backend/app/core/config.py expects.
# See backend/.env.example for the full list.

# Backend Configuration
BACKEND_URL=http://localhost:8000

# Frontend Configuration
FRONTEND_URL=http://localhost:3000

# Development Mode
NODE_ENV=development

# -----------------------------------------------------------------------------
# Custom Builder: Counter Ticket & Coverage Pack (backend/app/core/config.py)
# Flip one at a time: Counter first, then Coverage. Rollback = set to false.
# -----------------------------------------------------------------------------
# FEATURE_COUNTER_TICKET=true
# FEATURE_COVERAGE_PACK=true
