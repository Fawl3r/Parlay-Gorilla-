# Run health watchdog every minute. If checks fail twice in a row, restart parlaygorilla.
# Install: sudo cp docs/systemd/parlaygorilla-watchdog.{service,timer} /etc/systemd/system/
#          sudo systemctl daemon-reload
#          sudo systemctl enable --now parlaygorilla-watchdog.timer

[Unit]
Description=Run Parlay Gorilla health watchdog every minute

[Timer]
OnBootSec=60s
OnUnitActiveSec=60s
AccuracySec=10s
RandomizedDelaySec=10s
Persistent=true

[Install]
WantedBy=timers.target
