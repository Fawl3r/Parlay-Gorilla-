# /etc/parlaygorilla.env — template only (DO NOT commit real values)
# Copy to /etc/parlaygorilla.env on the Oracle VM and fill in secrets.
# chown root:root /etc/parlaygorilla.env && chmod 600 /etc/parlaygorilla.env

# Database (Render Postgres)
DATABASE_URL=postgresql+asyncpg://user:password@host/dbname?sslmode=require

# Redis (Render)
REDIS_URL=redis://red-xxx:6379

# Environment
ENVIRONMENT=production

# Telegram alerts (optional)
TELEGRAM_ALERTS_ENABLED=true
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALERT_CHAT_ID=

# Odds API
THE_ODDS_API_KEY=

# Add other app vars as needed (OPENAI_API_KEY, STRIPE_*, etc.)
