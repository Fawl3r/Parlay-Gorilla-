/**
 * Production mobile test routes.
 * App dashboard tabs; age gate is bypassed in wait helper.
 */
export const urls = {
  gorillaBuilder: "/app?tab=custom-builder",
  aiPicks: "/app?tab=ai-builder",
} as const;
