# ===========================================
# OCI (Oracle) production .env
# ===========================================
# Copy to /opt/parlaygorilla/.env on the Oracle VM.
# See docs/setup-oracle-and-cloudflare.md for full steps.
# Fill every value; copy additional keys from backend/.env.example (OpenAI, Resend, etc.) as needed.

ENVIRONMENT=production
DEBUG=false
USE_SQLITE=false

# Database (same as Render/Neon/Supabase)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Redis (API requires it in production; use Render Redis URL or a stub)
REDIS_URL=redis://red-xxx:6379

# OCI: DB-only verification (Python verifier polls DB; no Redis queue)
VERIFICATION_DELIVERY=db

# SUI (verifier + optional API)
SUI_RPC_URL=https://fullnode.mainnet.sui.io
SUI_PRIVATE_KEY=suiprivkey1...
SUI_PACKAGE_ID=0x...

# URLs (after DNS: api.parlaygorilla.com -> Oracle VM)
FRONTEND_URL=https://www.parlaygorilla.com
BACKEND_URL=https://api.parlaygorilla.com
APP_URL=https://www.parlaygorilla.com

# Required API keys (same as Render)
THE_ODDS_API_KEY=
OPENAI_API_KEY=
JWT_SECRET=

# Optional: API-Sports, Resend, Telegram, etc. (see backend/.env.example)
