[pytest]
asyncio_mode = auto
testpaths = tests
addopts = --import-mode=importlib
pythonpath = .
markers =
    integration: integration tests (external API/key); run with pytest -m integration; exclude in CI with -m "not integration"

