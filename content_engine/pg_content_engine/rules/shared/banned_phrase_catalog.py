from __future__ import annotations


class BannedPhraseCatalog:
    def phrases(self) -> list[str]:
        return [
            "lock",
            "guaranteed",
            "free money",
            "can't lose",
            "can’t lose",
            "max bet",
            "mortal lock",
            "easy money",
        ]
